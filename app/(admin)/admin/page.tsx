export const metadata = { robots: { index: false, follow: false } };
export const dynamic = "force-dynamic";

export default function AdminDashboardPage() {
  return <h1 className="text-2xl font-semibold text-primary">Admin</h1>;
}
